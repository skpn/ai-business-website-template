This project uses Astro + Tailwind.

Workflow:
- staging = preview site
- main = production site

Rules:
- Never push directly to main
- Always commit to staging
- Global UI lives in src/components
- Do not add frameworks or build tools
